{
  "name": "atlas",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run --prefix server dev\" \"bun run --cwd client dev\"",
    "build": "bun run --cwd client build",
    "start": "npm run --prefix server start",
    "lint": "biome lint .",
    "format": "biome format --write .",
    "check": "biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.4",
    "concurrently": "^8.2.2"
  }
}
